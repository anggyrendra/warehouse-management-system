# Base URL for the Warehouse API.
# During local dev, leave this unset so Vite's proxy forwards /api → backend.
# For production builds, set this to the full API URL, e.g.:
#   VITE_API_BASE_URL=https://api.example.com/api/v1
VITE_API_BASE_URL=/api/v1
